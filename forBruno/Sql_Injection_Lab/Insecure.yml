info:
  name: Insecure
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/login-insecure?username=admin&password=admin123
  params:
    - name: username
      value: admin
      type: query
    - name: password
      value: admin123
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
