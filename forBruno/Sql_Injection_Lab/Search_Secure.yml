info:
  name: Search_Secure
  type: http
  seq: 8

http:
  method: POST
  url: http://127.0.0.1:8010/search-secure
  body:
    type: json
    data: |-
      {
        "query": "' OR 1=1--"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
