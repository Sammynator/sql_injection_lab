info:
  name: Partial
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8010/login-partial
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
